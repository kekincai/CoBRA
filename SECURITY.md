# Security

CoBRA Web v0.1 は単一ユーザーのローカル利用を想定しています。
サーバーは `127.0.0.1` にバインドしてください。認証や利用者ごとの権限分離は実装していません。
インターネットや共有ネットワークに直接公開する構成は対象外です。

会社データは `data/company/` の SQLite に保存され、Git の管理対象から除外されます。
CSV と JSON バックアップには実績情報が含まれます。利用者が管理する非公開の場所で保管してください。
グラフライブラリは自己配信し、外部 CDN・アクセス解析・LLM API を呼びません。

Host 検証、書き込み時の Origin と JSON 検証、リクエストサイズ制限、CSP、CSV の数式対策を行います。
これは認証を代替する仕組みではありません。同じ OS アカウントからのローカルアクセスを信頼境界とします。

脆弱性は GitHub の private vulnerability reporting が利用可能な場合にそちらへ報告してください。
公開 Issue に実際の秘密情報・会社データ・認証情報を添付しないでください。
